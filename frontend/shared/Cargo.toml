[package]
name = "jalanaman-shared"
version = "0.1.0"
edition = "2021"

[lib]
name = "jalanaman_shared"
path = "src/lib.rs"

[dependencies]
# Dioxus with no platform feature — shared RSX compiles on all targets
dioxus = "0.6"

serde = { version = "1", features = ["derive"] }
serde_json = "1"
